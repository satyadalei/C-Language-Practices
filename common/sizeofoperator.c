/*size of the operand is calculated.*/
#include <stdio.h>
void main()
{
   float a = 2.5;
   int b = sizeof(a);
   int c = b*8 ;
   //float b = 2.345 ;
   //double c = 6.4 ;
   //char d = s ;
  // printf("a is equal to %f and it's data size is %d bytes and %d bits " ,a, b, c);
     printf("the size is %d",b);
}
